# LucidFence 2.0 · Hito M0 "Día 0" · Plan de implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Sustituir `main` de `adrimg3196/lucidfence` por un esqueleto Go 2.0 compilable y verde en CI, con el código 1.x preservado, el gate de CI completo, los raíles de agentes mínimos y la automatización local pausada.

**Architecture:** Un módulo Go (`github.com/adrimg3196/lucidfence`), un binario `cmd/lucidfence`, paquetes bajo `internal/` con fronteras validadas por `depguard` y por tests en `internal/arch`; frontend React bajo `web/` compilado a `internal/web/dist` y embebido con `embed.FS`; CI en un solo `ci.yml` con cinco jobs bloqueantes.

**Tech Stack:** Go 1.27.1, golangci-lint 2.13.2 (config v2), Node 24+ (local: 26.7), Vite 8.2, React 19.2, TypeScript 7.0, Tailwind 4.3, Vitest 5, GitHub Actions (`actions/checkout@v7`, `actions/setup-go@v7`, `actions/setup-node@v7`, `golangci/golangci-lint-action@v9`, `gitleaks/gitleaks-action@v3`, `actions/upload-artifact@v7`, `actions/download-artifact@v7`), `gh` CLI.

**Spec:** `docs/superpowers/specs/2026-09-05-lucidfence-2-go-rewrite-design.md` (secciones §5.1, §5.2, §5.8, §9, §10, §14 M0).

## Global Constraints

- Módulo Go: `github.com/adrimg3196/lucidfence`; directiva `go 1.27`.
- Dependencias Go permitidas (spec §5.8): `github.com/coreos/go-oidc/v3`, `golang.org/x/oauth2`, `golang.org/x/crypto`, `github.com/google/go-cmp`. Todo lo demás stdlib. En M0 no se añade ninguna.
- Límites físicos (spec §9.1): ningún `.go` > 400 líneas, ningún `.tsx` > 300, ninguna función Go > 60 líneas / 40 sentencias, complejidad ciclomática ≤ 15.
- Suelos de cobertura (spec §9.1): `internal/domain/**` e `internal/engine**` 85 %; resto 70 %; `cmd/battery` exento.
- Fronteras de importación: tabla de spec §5.2, reproducida en `ARCHITECTURE.md` y en `.golangci.yml`.
- Textos de UI y docs en español, sin emojis en UI; sin guiones largos en copy (regla taste-skill).
- Clon de trabajo: `/Users/adri/lucidfence-v2` con `user.email=adrimg3196@gmail.com`. **Nunca** `~/geofence-uem` ni `~/lucidfence`.
- Commits terminan con:
  ```
  Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
  Claude-Session: https://claude.ai/code/session_01GjMTkpr4PnhrZTzqQ7J75w
  ```
- Herramientas locales necesarias antes de empezar: `brew install go golangci-lint` (Go 1.27.1); Node ≥ 24 ya instalado; `gh auth status` OK.

---

## Mapa de ficheros del hito

| Fichero | Responsabilidad |
|---------|-----------------|
| `go.mod` | módulo y versión de Go |
| `cmd/lucidfence/main.go` | `main()` de 3 líneas → `run(args, stdout, stderr) int` |
| `cmd/lucidfence/main_test.go` | subcomando `version` y error de uso |
| `cmd/battery/main.go` | ejecuta la batería runtime contra un binario |
| `internal/version/version.go` | `Version`, `Commit`, `String()` |
| `internal/version/version_test.go` | formato de `String()` |
| `internal/web/web.go` | `Handler(fs.FS)` SPA con fallback y placeholder |
| `internal/web/web_test.go` | index, fallback, placeholder |
| `internal/web/dist/placeholder.html` | fichero siempre presente para `go:embed` |
| `internal/arch/arch_test.go` | límites de fichero, allowlists, paquetes documentados |
| `internal/arch/allowlist_go.txt`, `allowlist_npm.txt` | allowlists de dependencias |
| `internal/battery/battery.go` | tipos `Check`, `Env`, `Run` |
| `internal/battery/checks_m0.go` | check `version` |
| `internal/battery/battery_test.go` | `Run` cuenta PASS/FAIL |
| `.golangci.yml` | linters + depguard (fronteras) |
| `scripts/coverage.sh` | suelos de cobertura por paquete |
| `scripts/battery.sh` | compila y ejecuta la batería |
| `Makefile` | `build`, `web`, `lint`, `test`, `battery`, `verify` |
| `web/*` | esqueleto Vite + React + TS + Tailwind |
| `.github/workflows/ci.yml` | gate |
| `.github/workflows/agent-pr.yml`, `agent-automerge.yml` | raíles |
| `.github/CODEOWNERS`, `.github/pull_request_template.md` | protección y plantilla |
| `ARCHITECTURE.md`, `README.md`, `CHANGELOG.md`, `SECURITY.md`, `CONTRIBUTING.md` | docs de transición |

---

### Task 1: Preservar 1.x (tag y rama legacy)

**Files:** ninguno en el árbol; solo refs de git.

**Interfaces:**
- Produces: tag `v1.6.1-python-final` y rama `legacy/python` en `origin`, ambos apuntando al último `origin/main` 1.x.

- [ ] **Step 1: Situarse en el clon y refrescar `main`**

```bash
cd /Users/adri/lucidfence-v2
git fetch origin main
git log -1 --format='%h %cd %s' --date=short origin/main
```

Expected: una línea con el commit más reciente de `main` (fecha 2026-09-05 o posterior).

- [ ] **Step 2: Crear tag anotado y rama legacy**

```bash
git tag -a v1.6.1-python-final origin/main -m "LucidFence 1.x (Python, = release v1.6.1): último estado antes de la reescritura 2.0 en Go. Ver docs/superpowers/specs/2026-09-05-lucidfence-2-go-rewrite-design.md en la rama v2/design."
git branch legacy/python origin/main
```

- [ ] **Step 3: Publicar tag y rama**

```bash
git push origin v1.6.1-python-final legacy/python
```

- [ ] **Step 4: Verificar en GitHub**

```bash
gh api repos/adrimg3196/lucidfence/git/ref/tags/v1.6.1-python-final --jq .object.sha
gh api repos/adrimg3196/lucidfence/branches/legacy/python --jq .commit.sha
git rev-parse origin/main
```

Expected: los tres SHA coinciden.

---

### Task 2: Pausar la automatización local

**Files:**
- Mover: `~/Library/LaunchAgents/com.openmausbot.lf-daily.plist` → `.plist.disabled`
- Crear: `~/lucidfence-kanban-backup-2026-09-05.tar.gz` (export del tablero Hermes)

- [ ] **Step 1: Descargar el job launchd diario de LucidFence**

```bash
launchctl bootout "gui/$(id -u)/com.openmausbot.lf-daily" 2>/dev/null || launchctl unload ~/Library/LaunchAgents/com.openmausbot.lf-daily.plist
mv ~/Library/LaunchAgents/com.openmausbot.lf-daily.plist ~/Library/LaunchAgents/com.openmausbot.lf-daily.plist.disabled
launchctl list | grep -c "com.openmausbot.lf-daily" || true
```

Expected: la última línea imprime `0`.

- [ ] **Step 2: Exportar el tablero `lucidfence` de Hermes como copia de seguridad**

```bash
~/.hermes/hermes-agent/venv/bin/hermes kanban boards export lucidfence -o ~/lucidfence-kanban-backup-2026-09-05.tar.gz --include-logs
ls -la ~/lucidfence-kanban-backup-2026-09-05.tar.gz
```

Expected: fichero `.tar.gz` de tamaño > 0.

- [ ] **Step 3: Archivar el tablero (recuperable, no borrado)**

```bash
~/.hermes/hermes-agent/venv/bin/hermes kanban boards rm lucidfence
~/.hermes/hermes-agent/venv/bin/hermes kanban boards list
```

Expected: `lucidfence` ya no aparece en la lista (queda en `~/.hermes/kanban/boards/_archived/`).

- [ ] **Step 4: Anotar para el cierre**

Añadir al mensaje final al propietario: para reactivar el job diario, `mv` el `.disabled` de vuelta y `launchctl bootstrap gui/$(id -u) <plist>`; para recuperar el tablero, `hermes kanban boards import ~/lucidfence-kanban-backup-2026-09-05.tar.gz`.

---

### Task 3: Rama de trabajo y retirada del árbol Python

**Files:**
- Eliminar: todo el árbol excepto `LICENSE` y `docs/superpowers/specs/2026-09-05-lucidfence-2-go-rewrite-design.md`.

**Interfaces:**
- Produces: rama local `v2/day0` cuyo primer commit deja solo `LICENSE` y la spec.

- [ ] **Step 1: Crear la rama desde `origin/main` y traer la spec**

```bash
cd /Users/adri/lucidfence-v2
git checkout -q -b v2/day0 origin/main
git checkout v2/design -- docs/superpowers/specs/2026-09-05-lucidfence-2-go-rewrite-design.md
git status --short | head
```

Expected: la spec aparece como `A` (añadida).

- [ ] **Step 2: Retirar todo lo demás del índice y del disco**

```bash
git rm -r -q --cached .
git ls-files -z | xargs -0 rm -f 2>/dev/null || true
find . -mindepth 1 -maxdepth 1 ! -name .git ! -name LICENSE ! -name docs -exec rm -rf {} +
find docs -mindepth 1 -maxdepth 1 ! -name superpowers -exec rm -rf {} +
find docs/superpowers -mindepth 1 -maxdepth 1 ! -name specs -exec rm -rf {} +
git add -A
git status --short | grep -vE '^D ' 
```

Expected: solo `A  docs/superpowers/specs/...` y (sin cambios) `LICENSE`; el resto son `D`.

- [ ] **Step 3: Confirmar que quedan exactamente dos ficheros**

```bash
git ls-files
```

Expected:
```
LICENSE
docs/superpowers/specs/2026-09-05-lucidfence-2-go-rewrite-design.md
```

- [ ] **Step 4: Commit**

```bash
git commit -q -F - <<'MSG'
chore!: retirar el árbol Python 1.x (preservado en legacy/python y v1.6.1-python-final)

LucidFence 2.0 es una reescritura completa en Go. Este commit deja el
repositorio con la licencia y la spec de diseño aprobada; el esqueleto Go
llega en los commits siguientes del hito M0.

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01GjMTkpr4PnhrZTzqQ7J75w
MSG
git log --oneline -1
```

---

### Task 4: Esqueleto Go compilable con `version`

**Files:**
- Create: `go.mod`, `.gitignore`, `.editorconfig`, `.gitattributes`
- Create: `internal/version/version.go`, `internal/version/version_test.go`
- Create: `cmd/lucidfence/main.go`, `cmd/lucidfence/main_test.go`
- Create: `Makefile`

**Interfaces:**
- Produces: `version.Version string` (default `"2.0.0-dev"`), `version.Commit string` (default `"unknown"`), `func version.String() string` → `"lucidfence 2.0.0-dev (unknown, go1.27.1, darwin/arm64)"`.
- Produces: `func run(args []string, stdout, stderr io.Writer) int` en `cmd/lucidfence`; subcomandos conocidos: `version`. M1 añade `serve`, `doctor`, `open`.

- [ ] **Step 1: Instalar Go y comprobar versión**

```bash
brew install go golangci-lint
go version
golangci-lint version
```

Expected: `go version go1.27.1 darwin/arm64` y `golangci-lint has version 2.13.2`.

- [ ] **Step 2: Crear `go.mod`, `.gitignore`, `.editorconfig`, `.gitattributes`**

```bash
cd /Users/adri/lucidfence-v2
cat > go.mod <<'EOF'
module github.com/adrimg3196/lucidfence

go 1.27
EOF
cat > .gitignore <<'EOF'
# binarios y artefactos
/bin/
/dist/
coverage.out
test.out
# frontend
web/node_modules/
web/dist/
web/playwright-report/
web/test-results/
internal/web/dist/*
!internal/web/dist/placeholder.html
# datos locales de desarrollo
/data/
/config.json
.env
# sistema
.DS_Store
EOF
cat > .editorconfig <<'EOF'
root = true
[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true
indent_style = space
indent_size = 2
[*.go]
indent_style = tab
[Makefile]
indent_style = tab
EOF
printf '* text=auto eol=lf\n' > .gitattributes
```

- [ ] **Step 3: Test de `version.String()` (falla)**

`internal/version/version_test.go`:

```go
package version

import (
	"runtime"
	"strings"
	"testing"
)

func TestStringIncluyeVersionCommitYPlataforma(t *testing.T) {
	got := String()
	for _, want := range []string{"lucidfence ", Version, Commit, runtime.Version(), runtime.GOOS + "/" + runtime.GOARCH} {
		if !strings.Contains(got, want) {
			t.Fatalf("String() = %q, no contiene %q", got, want)
		}
	}
}

func TestVersionPorDefectoEsDev(t *testing.T) {
	if Version != "2.0.0-dev" {
		t.Fatalf("Version por defecto = %q, quiero 2.0.0-dev", Version)
	}
}
```

- [ ] **Step 4: Ejecutar y ver que falla**

Run: `go test ./internal/version/`
Expected: error de compilación `undefined: String` (o `Version`).

- [ ] **Step 5: Implementar `internal/version/version.go`**

```go
// Package version expone la versión del binario. Los valores se fijan en
// build con -ldflags "-X .../internal/version.Version=2.0.0 -X .../internal/version.Commit=abc".
package version

import (
	"fmt"
	"runtime"
)

// Version es la versión semántica del binario. "2.0.0-dev" en builds locales.
var Version = "2.0.0-dev"

// Commit es el SHA corto del commit de build. "unknown" en builds locales.
var Commit = "unknown"

// String devuelve la línea que imprime `lucidfence version`.
func String() string {
	return fmt.Sprintf("lucidfence %s (%s, %s, %s/%s)", Version, Commit, runtime.Version(), runtime.GOOS, runtime.GOARCH)
}
```

- [ ] **Step 6: Ejecutar tests (pasan)**

Run: `go test ./internal/version/ -v`
Expected: `PASS` en los dos tests.

- [ ] **Step 7: Test del binario (falla)**

`cmd/lucidfence/main_test.go`:

```go
package main

import (
	"bytes"
	"strings"
	"testing"
)

func TestRunVersionImprimeVersion(t *testing.T) {
	var out, errb bytes.Buffer
	code := run([]string{"version"}, &out, &errb)
	if code != 0 {
		t.Fatalf("exit=%d stderr=%q", code, errb.String())
	}
	if !strings.HasPrefix(out.String(), "lucidfence 2.0.0-dev") {
		t.Fatalf("stdout=%q", out.String())
	}
}

func TestRunSinArgumentosMuestraUsoYFalla(t *testing.T) {
	var out, errb bytes.Buffer
	if code := run(nil, &out, &errb); code != 2 {
		t.Fatalf("exit=%d, quiero 2", code)
	}
	if !strings.Contains(errb.String(), "uso: lucidfence") {
		t.Fatalf("stderr=%q", errb.String())
	}
}

func TestRunSubcomandoDesconocido(t *testing.T) {
	var out, errb bytes.Buffer
	if code := run([]string{"nada"}, &out, &errb); code != 2 {
		t.Fatalf("exit=%d, quiero 2", code)
	}
	if !strings.Contains(errb.String(), `subcomando desconocido "nada"`) {
		t.Fatalf("stderr=%q", errb.String())
	}
}
```

- [ ] **Step 8: Ejecutar y ver que falla**

Run: `go test ./cmd/lucidfence/`
Expected: `undefined: run`.

- [ ] **Step 9: Implementar `cmd/lucidfence/main.go`**

```go
// Command lucidfence es el binario único de LucidFence 2.0: servidor,
// dashboard, CLI y MCP. main() solo despacha subcomandos; la lógica vive en
// internal/.
package main

import (
	"fmt"
	"io"
	"os"

	"github.com/adrimg3196/lucidfence/internal/version"
)

const usage = `uso: lucidfence <subcomando> [opciones]

subcomandos:
  version   imprime la versión del binario
`

type command func(args []string, stdout, stderr io.Writer) int

func commands() map[string]command {
	return map[string]command{
		"version": runVersion,
	}
}

func run(args []string, stdout, stderr io.Writer) int {
	if len(args) == 0 {
		fmt.Fprint(stderr, usage)
		return 2
	}
	cmd, ok := commands()[args[0]]
	if !ok {
		fmt.Fprintf(stderr, "subcomando desconocido %q\n\n%s", args[0], usage)
		return 2
	}
	return cmd(args[1:], stdout, stderr)
}

func runVersion(_ []string, stdout, _ io.Writer) int {
	fmt.Fprintln(stdout, version.String())
	return 0
}

func main() {
	os.Exit(run(os.Args[1:], os.Stdout, os.Stderr))
}
```

- [ ] **Step 10: Ejecutar tests (pasan) y compilar**

Run: `go test ./... && go build -o bin/lucidfence ./cmd/lucidfence && ./bin/lucidfence version`
Expected: tests `ok`, y la línea `lucidfence 2.0.0-dev (unknown, go1.27.1, darwin/arm64)`.

- [ ] **Step 11: `Makefile`**

```make
MODULE := github.com/adrimg3196/lucidfence
VERSION ?= 2.0.0-dev
COMMIT ?= $(shell git rev-parse --short HEAD 2>/dev/null || echo unknown)
LDFLAGS := -s -w -X $(MODULE)/internal/version.Version=$(VERSION) -X $(MODULE)/internal/version.Commit=$(COMMIT)

.PHONY: build web lint test cover battery verify clean

web:
	cd web && npm ci && npm run build

build:
	CGO_ENABLED=0 go build -trimpath -ldflags '$(LDFLAGS)' -o bin/lucidfence ./cmd/lucidfence

lint:
	gofmt -l . | tee /dev/stderr | test -z "$$(cat)"
	go vet ./...
	golangci-lint run ./...

test:
	go test -race -count=1 ./...

cover:
	scripts/coverage.sh

battery: build
	scripts/battery.sh bin/lucidfence

verify: lint cover web battery
	@echo "verify: OK"

clean:
	rm -rf bin coverage.out test.out
```

- [ ] **Step 12: Commit**

```bash
gofmt -l . ; go vet ./...
git add go.mod .gitignore .editorconfig .gitattributes Makefile cmd internal
git commit -q -m "feat(go): esqueleto del binario lucidfence con subcomando version

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01GjMTkpr4PnhrZTzqQ7J75w"
```

---

### Task 5: Fronteras de arquitectura, límites físicos y allowlists

**Files:**
- Create: `.golangci.yml`
- Create: `internal/arch/arch_test.go`, `internal/arch/allowlist_go.txt`, `internal/arch/allowlist_npm.txt`, `internal/arch/doc.go`
- Create: `ARCHITECTURE.md`

**Interfaces:**
- Produces: `ARCHITECTURE.md` con una tabla "## Paquetes" cuya primera columna es la ruta de cada paquete bajo `internal/` y `cmd/`; el test exige que todo paquete con ficheros `.go` esté listado.
- Produces: allowlists, una entrada por línea, comentarios con `#`.

> **Desviación explícita respecto a la spec §9.1 paso 2:** los límites físicos no viven en `scripts/limits.sh` sino en tests Go de `internal/arch` (corren con `go test`, sin bash, y fallan la CI igual). CODEOWNERS protege `internal/arch/` en lugar de `scripts/limits.sh`. `funlen`/`gocyclo` de golangci-lint cubren el límite por función.

- [ ] **Step 1: `.golangci.yml` (formato v2) con depguard en modo estricto**

```yaml
version: "2"

run:
  timeout: 5m

linters:
  default: standard
  enable:
    - depguard
    - funlen
    - gocyclo
    - misspell
    - unparam
  settings:
    funlen:
      lines: 60
      statements: 40
    gocyclo:
      min-complexity: 15
    depguard:
      rules:
        domain:
          list-mode: strict
          files: ["**/internal/domain/**"]
          allow: ["$gostd", "github.com/adrimg3196/lucidfence/internal/domain"]
        uem:
          list-mode: strict
          files: ["**/internal/uem/**"]
          allow: ["$gostd", "github.com/adrimg3196/lucidfence/internal/domain", "github.com/adrimg3196/lucidfence/internal/uem"]
        store:
          list-mode: strict
          files: ["**/internal/store/**"]
          allow: ["$gostd", "github.com/adrimg3196/lucidfence/internal/domain"]
        leaf-services:
          list-mode: strict
          files: ["**/internal/notify/**", "**/internal/posture/**", "**/internal/reports/**"]
          allow: ["$gostd", "github.com/adrimg3196/lucidfence/internal/domain", "github.com/adrimg3196/lucidfence/internal/store"]
        engine:
          list-mode: strict
          files: ["**/internal/engine/**"]
          allow:
            - "$gostd"
            - "github.com/adrimg3196/lucidfence/internal/domain"
            - "github.com/adrimg3196/lucidfence/internal/uem"
            - "github.com/adrimg3196/lucidfence/internal/store"
            - "github.com/adrimg3196/lucidfence/internal/notify"
            - "github.com/adrimg3196/lucidfence/internal/posture"
            - "github.com/adrimg3196/lucidfence/internal/config"
        auth:
          list-mode: strict
          files: ["**/internal/auth/**"]
          allow: ["$gostd", "github.com/adrimg3196/lucidfence/internal/domain", "github.com/adrimg3196/lucidfence/internal/store", "golang.org/x/crypto"]
        api:
          list-mode: strict
          files: ["**/internal/api/**"]
          allow:
            - "$gostd"
            - "github.com/adrimg3196/lucidfence/internal/engine"
            - "github.com/adrimg3196/lucidfence/internal/auth"
            - "github.com/adrimg3196/lucidfence/internal/store"
            - "github.com/adrimg3196/lucidfence/internal/reports"
            - "github.com/adrimg3196/lucidfence/internal/domain"
            - "github.com/adrimg3196/lucidfence/internal/uem"
            - "github.com/adrimg3196/lucidfence/internal/config"
            - "github.com/adrimg3196/lucidfence/internal/version"
          deny:
            - pkg: "github.com/adrimg3196/lucidfence/internal/uem/"
              desc: "api nunca importa un conector concreto (spec §5.2)"
        mcp:
          list-mode: strict
          files: ["**/internal/mcp/**"]
          allow: ["$gostd", "github.com/adrimg3196/lucidfence/internal/api"]
        leaf-utils:
          list-mode: strict
          files: ["**/internal/web/**", "**/internal/version/**", "**/internal/config/**", "**/internal/arch/**", "**/internal/battery/**"]
          allow: ["$gostd"]

formatters:
  enable:
    - gofmt
    - goimports
```

- [ ] **Step 2: Allowlists**

`internal/arch/allowlist_go.txt`:
```
# Dependencias Go permitidas (spec §5.8). Una por línea. Cambiar este fichero exige aprobación del propietario (CODEOWNERS).
github.com/coreos/go-oidc/v3
golang.org/x/oauth2
golang.org/x/crypto
github.com/google/go-cmp
```

`internal/arch/allowlist_npm.txt`:
```
# Dependencias npm permitidas (spec §7.2), runtime y desarrollo. Una por línea.
react
react-dom
react-router
@tanstack/react-query
tailwindcss
@tailwindcss/vite
@phosphor-icons/react
motion
maplibre-gl
recharts
react-hook-form
@hookform/resolvers
zod
openapi-fetch
geist
class-variance-authority
clsx
tailwind-merge
radix-ui
@radix-ui/react-slot
vite
@vitejs/plugin-react
typescript
vitest
@vitest/coverage-v8
jsdom
@testing-library/react
@testing-library/jest-dom
@testing-library/user-event
@playwright/test
openapi-typescript
eslint
typescript-eslint
@eslint/js
eslint-plugin-react-hooks
eslint-plugin-react-refresh
prettier
globals
@types/react
@types/react-dom
@types/node
```

- [ ] **Step 3: Test de arquitectura (falla porque `ARCHITECTURE.md` no existe)**

`internal/arch/doc.go`:
```go
// Package arch contiene solo tests que hacen cumplir las reglas físicas del
// repositorio: límites de tamaño, allowlists de dependencias y paquetes
// documentados en ARCHITECTURE.md. No tiene código de producto.
package arch
```

`internal/arch/arch_test.go`:
```go
package arch

import (
	"bufio"
	"encoding/json"
	"os"
	"path/filepath"
	"regexp"
	"strings"
	"testing"
)

const (
	maxGoLines  = 400
	maxTSXLines = 300
)

func repoRoot(t *testing.T) string {
	t.Helper()
	dir, err := os.Getwd()
	if err != nil {
		t.Fatal(err)
	}
	for {
		if _, err := os.Stat(filepath.Join(dir, "go.mod")); err == nil {
			return dir
		}
		parent := filepath.Dir(dir)
		if parent == dir {
			t.Fatal("no encuentro go.mod hacia arriba")
		}
		dir = parent
	}
}

func countLines(t *testing.T, path string) int {
	t.Helper()
	f, err := os.Open(path)
	if err != nil {
		t.Fatal(err)
	}
	defer f.Close()
	n := 0
	sc := bufio.NewScanner(f)
	sc.Buffer(make([]byte, 1024*1024), 1024*1024)
	for sc.Scan() {
		n++
	}
	return n
}

func skipDir(name string) bool {
	return name == ".git" || name == "node_modules" || name == "dist" || name == "bin" || name == ".claude"
}

func firstLineAllows(t *testing.T, path string) bool {
	t.Helper()
	f, err := os.Open(path)
	if err != nil {
		t.Fatal(err)
	}
	defer f.Close()
	sc := bufio.NewScanner(f)
	if sc.Scan() {
		return regexp.MustCompile(`limits:allow #\d+`).MatchString(sc.Text())
	}
	return false
}

func TestFileLimits(t *testing.T) {
	root := repoRoot(t)
	err := filepath.WalkDir(root, func(path string, d os.DirEntry, err error) error {
		if err != nil {
			return err
		}
		if d.IsDir() && skipDir(d.Name()) {
			return filepath.SkipDir
		}
		if d.IsDir() {
			return nil
		}
		limit := 0
		switch {
		case strings.HasSuffix(path, ".go"):
			limit = maxGoLines
		case strings.HasSuffix(path, ".tsx"):
			limit = maxTSXLines
		default:
			return nil
		}
		if n := countLines(t, path); n > limit && !firstLineAllows(t, path) {
			t.Errorf("%s: %d líneas > %d (spec §9.1). Divide el fichero o añade `limits:allow #<issue>` en la primera línea", path, n, limit)
		}
		return nil
	})
	if err != nil {
		t.Fatal(err)
	}
}

func readAllowlist(t *testing.T, path string) map[string]bool {
	t.Helper()
	f, err := os.Open(path)
	if err != nil {
		t.Fatal(err)
	}
	defer f.Close()
	out := map[string]bool{}
	sc := bufio.NewScanner(f)
	for sc.Scan() {
		line := strings.TrimSpace(sc.Text())
		if line == "" || strings.HasPrefix(line, "#") {
			continue
		}
		out[line] = true
	}
	return out
}

func TestGoDependencyAllowlist(t *testing.T) {
	root := repoRoot(t)
	allowed := readAllowlist(t, filepath.Join(root, "internal/arch/allowlist_go.txt"))
	data, err := os.ReadFile(filepath.Join(root, "go.mod"))
	if err != nil {
		t.Fatal(err)
	}
	re := regexp.MustCompile(`(?m)^\s*([a-zA-Z0-9./_-]+)\s+v[0-9][^\s]*`)
	for _, m := range re.FindAllStringSubmatch(string(data), -1) {
		mod := m[1]
		if mod == "module" || mod == "go" || mod == "toolchain" {
			continue
		}
		if !allowed[mod] {
			t.Errorf("go.mod requiere %q, que no está en internal/arch/allowlist_go.txt (spec §5.8)", mod)
		}
	}
}

func TestNpmDependencyAllowlist(t *testing.T) {
	root := repoRoot(t)
	pkgPath := filepath.Join(root, "web/package.json")
	data, err := os.ReadFile(pkgPath)
	if err != nil {
		t.Skipf("web/package.json no existe todavía: %v", err)
	}
	allowed := readAllowlist(t, filepath.Join(root, "internal/arch/allowlist_npm.txt"))
	var pkg struct {
		Dependencies    map[string]string `json:"dependencies"`
		DevDependencies map[string]string `json:"devDependencies"`
	}
	if err := json.Unmarshal(data, &pkg); err != nil {
		t.Fatal(err)
	}
	for _, deps := range []map[string]string{pkg.Dependencies, pkg.DevDependencies} {
		for name := range deps {
			if !allowed[name] {
				t.Errorf("web/package.json depende de %q, que no está en internal/arch/allowlist_npm.txt (spec §7.2)", name)
			}
		}
	}
}

func goPackages(t *testing.T, root string) []string {
	t.Helper()
	seen := map[string]bool{}
	var pkgs []string
	err := filepath.WalkDir(root, func(path string, d os.DirEntry, err error) error {
		if err != nil {
			return err
		}
		if d.IsDir() && skipDir(d.Name()) {
			return filepath.SkipDir
		}
		if d.IsDir() || !strings.HasSuffix(path, ".go") {
			return nil
		}
		rel, _ := filepath.Rel(root, filepath.Dir(path))
		if !(strings.HasPrefix(rel, "internal") || strings.HasPrefix(rel, "cmd")) || seen[rel] {
			return nil
		}
		seen[rel] = true
		pkgs = append(pkgs, filepath.ToSlash(rel))
		return nil
	})
	if err != nil {
		t.Fatal(err)
	}
	return pkgs
}

func TestArchitectureDocListsEveryPackage(t *testing.T) {
	root := repoRoot(t)
	doc, err := os.ReadFile(filepath.Join(root, "ARCHITECTURE.md"))
	if err != nil {
		t.Fatalf("ARCHITECTURE.md obligatorio (spec §9.2): %v", err)
	}
	for _, pkg := range goPackages(t, root) {
		if !strings.Contains(string(doc), "`"+pkg+"`") {
			t.Errorf("el paquete %s no aparece en ARCHITECTURE.md; documenta su responsabilidad", pkg)
		}
	}
}
```

- [ ] **Step 4: Ejecutar y ver que falla**

Run: `go test ./internal/arch/ -run TestArchitectureDocListsEveryPackage`
Expected: FAIL con `ARCHITECTURE.md obligatorio`.

- [ ] **Step 5: Escribir `ARCHITECTURE.md`**

```markdown
# Arquitectura de LucidFence 2.0

Un módulo Go, un binario. Este documento es normativo: `internal/arch` y
`depguard` (`.golangci.yml`) fallan la CI si el código se desvía de él. La
spec completa está en `docs/superpowers/specs/2026-09-05-lucidfence-2-go-rewrite-design.md`.

## Principios

1. Local-first: el dato del tenant vive en la máquina del tenant. Cero telemetría.
2. Complemento del UEM, nunca un UEM.
3. El runtime lo decide el admin: `observe` por defecto, doble llave para `wipe`.
4. Gratis y open source (Apache-2.0).
5. Todo claim se verifica en vivo contra el binario (batería runtime).
6. Estado en JSON/JSONL en disco, sin base de datos.

## Paquetes

| Paquete | Responsabilidad |
|---------|-----------------|
| `cmd/lucidfence` | `main()` y despacho de subcomandos. Sin lógica de negocio. |
| `cmd/battery` | Ejecuta la batería runtime contra un binario compilado. Solo CI y desarrollo. |
| `internal/version` | Versión y commit del binario, fijados por `-ldflags`. |
| `internal/web` | `embed.FS` del frontend compilado y handler SPA con fallback. |
| `internal/arch` | Tests que hacen cumplir límites físicos, allowlists y este documento. |
| `internal/battery` | Checks en vivo (`RUNTIME: N/N`). Cada claim de producto añade uno. |

Los paquetes de M1 en adelante (`internal/domain/...`, `internal/engine`,
`internal/uem/...`, `internal/store`, `internal/auth`, `internal/api`,
`internal/notify`, `internal/posture`, `internal/reports`, `internal/mcp`,
`internal/config`, `internal/migrate`) se añaden a esta tabla en el commit que
los crea; el test `TestArchitectureDocListsEveryPackage` lo exige.

## Reglas de dependencia

| Paquete | Puede importar del proyecto |
|---------|-----------------------------|
| `internal/domain` | nada (solo stdlib y otros subpaquetes de `domain`) |
| `internal/uem` y conectores | `domain`, `uem` |
| `internal/store` | `domain` |
| `internal/notify`, `internal/posture`, `internal/reports` | `domain`, `store` |
| `internal/engine` | `domain`, `uem`, `store`, `notify`, `posture`, `config` |
| `internal/auth` | `domain`, `store` (+ `golang.org/x/crypto`) |
| `internal/api` | `engine`, `auth`, `store`, `reports`, `domain`, `uem` (nunca un conector concreto), `config`, `version` |
| `internal/mcp` | `api` (cliente HTTP local) |
| `internal/web`, `internal/version`, `internal/config`, `internal/arch`, `internal/battery` | solo stdlib |
| `cmd/*` | todo |

## Límites físicos

- Ficheros Go ≤ 400 líneas; componentes `.tsx` ≤ 300 líneas. Excepción solo con
  `// limits:allow #<issue>` en la primera línea.
- Funciones Go ≤ 60 líneas y ≤ 40 sentencias; complejidad ciclomática ≤ 15.
- Dependencias externas solo las de `internal/arch/allowlist_go.txt` y
  `internal/arch/allowlist_npm.txt`.

## Ficheros protegidos (CODEOWNERS)

`ARCHITECTURE.md`, `.github/`, `go.mod`, `web/package.json`,
`internal/arch/allowlist_*.txt`, `internal/engine/guardrails*.go`. Cualquier
cambio exige aprobación del propietario aunque la CI esté verde.
```

- [ ] **Step 6: Ejecutar lint y tests (pasan)**

Run: `golangci-lint run ./... && go test ./internal/arch/ -v`
Expected: lint sin hallazgos; `TestFileLimits`, `TestGoDependencyAllowlist`, `TestArchitectureDocListsEveryPackage` PASS; `TestNpmDependencyAllowlist` SKIP (hasta la Task 6).

- [ ] **Step 7: Commit**

```bash
git add .golangci.yml internal/arch ARCHITECTURE.md
git commit -q -m "chore(arch): fronteras depguard, límites físicos, allowlists y ARCHITECTURE.md

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01GjMTkpr4PnhrZTzqQ7J75w"
```

---

### Task 6: Esqueleto del frontend y embed en el binario

**Files:**
- Create: `web/package.json`, `web/vite.config.ts`, `web/tsconfig.json`, `web/tsconfig.node.json`, `web/index.html`, `web/eslint.config.js`, `web/vitest.config.ts`, `web/src/main.tsx`, `web/src/app/App.tsx`, `web/src/app/App.test.tsx`, `web/src/styles/tokens.css`, `web/src/test/setup.ts`, `web/scripts/copy-fonts.mjs`
- Create: `internal/web/web.go`, `internal/web/web_test.go`, `internal/web/dist/placeholder.html`

**Interfaces:**
- Produces: `web.Dist() fs.FS` (subárbol `dist` embebido) y `web.Handler(fsys fs.FS) http.Handler`: sirve ficheros estáticos; para rutas sin fichero devuelve `index.html` si existe, o `placeholder.html` con estado 200 y la cadena `LucidFence: frontend no compilado`.
- Produces: `npm run build` escribe en `../internal/web/dist` sin borrar `placeholder.html`.

- [ ] **Step 1: Test del handler (falla)**

`internal/web/web_test.go`:
```go
package web

import (
	"net/http"
	"net/http/httptest"
	"strings"
	"testing"
	"testing/fstest"
)

func TestHandlerSirveIndexYFallbackSPA(t *testing.T) {
	fsys := fstest.MapFS{
		"index.html":    {Data: []byte(`<div id="root"></div>`)},
		"assets/app.js": {Data: []byte(`console.log(1)`)},
	}
	h := Handler(fsys)
	for _, path := range []string{"/", "/devices/abc", "/index.html"} {
		rr := httptest.NewRecorder()
		h.ServeHTTP(rr, httptest.NewRequest(http.MethodGet, path, nil))
		if rr.Code != 200 || !strings.Contains(rr.Body.String(), `id="root"`) {
			t.Fatalf("%s: code=%d body=%q", path, rr.Code, rr.Body.String())
		}
	}
	rr := httptest.NewRecorder()
	h.ServeHTTP(rr, httptest.NewRequest(http.MethodGet, "/assets/app.js", nil))
	if rr.Code != 200 || rr.Body.String() != "console.log(1)" {
		t.Fatalf("asset: code=%d body=%q", rr.Code, rr.Body.String())
	}
}

func TestHandlerSinBuildSirvePlaceholder(t *testing.T) {
	h := Handler(Dist())
	rr := httptest.NewRecorder()
	h.ServeHTTP(rr, httptest.NewRequest(http.MethodGet, "/", nil))
	if rr.Code != 200 {
		t.Fatalf("code=%d", rr.Code)
	}
	body := rr.Body.String()
	if !strings.Contains(body, "LucidFence") {
		t.Fatalf("body=%q", body)
	}
}

func TestIsBuiltDetectaIndex(t *testing.T) {
	if IsBuilt(fstest.MapFS{"placeholder.html": {Data: []byte("x")}}) {
		t.Fatal("solo placeholder no cuenta como build")
	}
	if !IsBuilt(fstest.MapFS{"index.html": {Data: []byte("x")}}) {
		t.Fatal("index.html cuenta como build")
	}
}
```

- [ ] **Step 2: Ejecutar y ver que falla**

Run: `go test ./internal/web/`
Expected: `undefined: Handler` (y `Dist`, `IsBuilt`).

- [ ] **Step 3: Implementar `internal/web/web.go` y el placeholder**

`internal/web/dist/placeholder.html`:
```html
<!doctype html>
<html lang="es"><head><meta charset="utf-8"><title>LucidFence</title></head>
<body style="font-family:system-ui;padding:2rem;color:#20241F;background:#FAFAF8">
<h1>LucidFence: frontend no compilado</h1>
<p>Este binario se compiló sin el dashboard. Ejecuta <code>make web build</code> y vuelve a arrancar.</p>
</body></html>
```

`internal/web/web.go`:
```go
// Package web embebe el dashboard compilado (web/ → internal/web/dist) y lo
// sirve como SPA. placeholder.html está siempre en git para que go:embed
// compile aunque el frontend no se haya construido.
package web

import (
	"embed"
	"io/fs"
	"net/http"
	"strings"
)

//go:embed all:dist
var dist embed.FS

// Dist devuelve el subárbol embebido (index.html, assets/...).
func Dist() fs.FS {
	sub, err := fs.Sub(dist, "dist")
	if err != nil {
		panic(err) // solo si el embed está roto en compilación
	}
	return sub
}

// IsBuilt indica si el árbol contiene un index.html real (no solo el placeholder).
func IsBuilt(fsys fs.FS) bool {
	_, err := fs.Stat(fsys, "index.html")
	return err == nil
}

// Handler sirve ficheros estáticos y hace fallback SPA a index.html; sin
// build sirve placeholder.html para que /api siga siendo verificable.
func Handler(fsys fs.FS) http.Handler {
	files := http.FS(fsys)
	fallback := "index.html"
	if !IsBuilt(fsys) {
		fallback = "placeholder.html"
	}
	return http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		name := strings.TrimPrefix(r.URL.Path, "/")
		if name == "" || name == "index.html" {
			name = fallback
		}
		if _, err := fs.Stat(fsys, name); err != nil {
			name = fallback
		}
		if strings.HasPrefix(name, "assets/") {
			w.Header().Set("Cache-Control", "public, max-age=31536000, immutable")
		} else {
			w.Header().Set("Cache-Control", "no-cache")
		}
		r2 := r.Clone(r.Context())
		r2.URL.Path = "/" + name
		http.FileServer(files).ServeHTTP(w, r2)
	})
}
```

- [ ] **Step 4: Ejecutar tests (pasan)**

Run: `go test ./internal/web/ -v`
Expected: 3 PASS.

- [ ] **Step 5: `web/package.json` con versiones fijadas**

```json
{
  "name": "lucidfence-web",
  "private": true,
  "version": "2.0.0-dev",
  "type": "module",
  "scripts": {
    "postinstall": "node scripts/copy-fonts.mjs",
    "dev": "vite",
    "build": "tsc --noEmit -p tsconfig.json && vite build",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "react": "19.2.8",
    "react-dom": "19.2.8",
    "geist": "1.7.2"
  },
  "devDependencies": {
    "@eslint/js": "9.39.0",
    "@testing-library/jest-dom": "7.0.1",
    "@testing-library/react": "16.3.3",
    "@types/node": "24.10.0",
    "@types/react": "19.2.5",
    "@types/react-dom": "19.2.3",
    "@vitejs/plugin-react": "6.1.1",
    "@vitest/coverage-v8": "5.0.0",
    "@tailwindcss/vite": "4.3.3",
    "eslint": "10.10.0",
    "eslint-plugin-react-hooks": "7.0.0",
    "eslint-plugin-react-refresh": "0.4.24",
    "globals": "16.5.0",
    "jsdom": "30.0.1",
    "prettier": "3.9.6",
    "tailwindcss": "4.3.3",
    "typescript": "7.0.2",
    "typescript-eslint": "8.69.0",
    "vite": "8.2.2",
    "vitest": "5.0.0"
  }
}
```

Si `npm install` rechaza alguna versión exacta de las devDependencies auxiliares (`@eslint/js`, `@types/*`, `eslint-plugin-*`, `globals`), sustituir por la última publicada con `npm view <paquete> version` y dejar el lock como fuente de verdad.

- [ ] **Step 6: Configuración de Vite, TypeScript, ESLint, Vitest y fuentes**

`web/vite.config.ts`:
```ts
import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";
import tailwindcss from "@tailwindcss/vite";
import { fileURLToPath, URL } from "node:url";

export default defineConfig({
  plugins: [react(), tailwindcss()],
  resolve: { alias: { "@": fileURLToPath(new URL("./src", import.meta.url)) } },
  build: {
    outDir: "../internal/web/dist",
    emptyOutDir: false,
    sourcemap: false,
  },
  server: { proxy: { "/api": "http://127.0.0.1:8765" } },
});
```

`web/tsconfig.json`:
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "skipLibCheck": true,
    "isolatedModules": true,
    "resolveJsonModule": true,
    "types": ["vitest/globals", "@testing-library/jest-dom"],
    "baseUrl": ".",
    "paths": { "@/*": ["src/*"] }
  },
  "include": ["src", "vite.config.ts", "vitest.config.ts"]
}
```

`web/vitest.config.ts`:
```ts
import { defineConfig, mergeConfig } from "vitest/config";
import viteConfig from "./vite.config";

export default mergeConfig(
  viteConfig,
  defineConfig({
    test: {
      environment: "jsdom",
      globals: true,
      setupFiles: ["./src/test/setup.ts"],
      css: false,
    },
  }),
);
```

`web/src/test/setup.ts`:
```ts
import "@testing-library/jest-dom/vitest";
```

`web/eslint.config.js`:
```js
import js from "@eslint/js";
import globals from "globals";
import reactHooks from "eslint-plugin-react-hooks";
import reactRefresh from "eslint-plugin-react-refresh";
import tseslint from "typescript-eslint";

export default tseslint.config(
  { ignores: ["dist", "../internal/web/dist"] },
  {
    extends: [js.configs.recommended, ...tseslint.configs.recommended],
    files: ["**/*.{ts,tsx}"],
    languageOptions: { ecmaVersion: 2022, globals: globals.browser },
    plugins: { "react-hooks": reactHooks, "react-refresh": reactRefresh },
    rules: {
      ...reactHooks.configs.recommended.rules,
      "react-refresh/only-export-components": ["warn", { allowConstantExport: true }],
    },
  },
);
```

`web/scripts/copy-fonts.mjs` (Geist autoalojada, sin Google Fonts):
```js
import { copyFileSync, mkdirSync } from "node:fs";
import { dirname, join } from "node:path";
import { fileURLToPath } from "node:url";

const here = dirname(fileURLToPath(import.meta.url));
const src = join(here, "..", "node_modules", "geist", "dist", "fonts");
const dst = join(here, "..", "public", "fonts");
mkdirSync(dst, { recursive: true });
for (const f of ["geist-sans/Geist-Variable.woff2", "geist-sans/Geist-Italic[wght].woff2", "geist-mono/GeistMono-Variable.woff2"]) {
  copyFileSync(join(src, f), join(dst, f.split("/")[1]));
}
console.log("fonts: Geist copiadas a public/fonts");
```

`web/index.html`:
```html
<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="color-scheme" content="light dark" />
    <title>LucidFence</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

- [ ] **Step 7: Tokens de diseño (paleta Forest, spec §7.1) y `App` mínima**

`web/src/styles/tokens.css`:
```css
@import "tailwindcss";

@font-face { font-family: "Geist"; src: url("/fonts/Geist-Variable.woff2") format("woff2"); font-weight: 100 900; font-display: swap; }
@font-face { font-family: "Geist"; src: url("/fonts/Geist-Italic[wght].woff2") format("woff2"); font-weight: 100 900; font-style: italic; font-display: swap; }
@font-face { font-family: "Geist Mono"; src: url("/fonts/GeistMono-Variable.woff2") format("woff2"); font-weight: 100 900; font-display: swap; }

@theme {
  --font-sans: "Geist", ui-sans-serif, system-ui, sans-serif;
  --font-mono: "Geist Mono", ui-monospace, SFMono-Regular, monospace;
  --radius-ui: 8px;
  --color-bg: #FAFAF8;
  --color-bg-2: #F4F3F0;
  --color-panel: #FFFFFF;
  --color-border: #E8E7E2;
  --color-fg: #20241F;
  --color-fg-2: #3C403B;
  --color-muted: #5E635C;
  --color-accent: #3E7A5E;
  --color-accent-h: #346951;
  --color-accent-fg: #FFFFFF;
  --color-sev-low: #346538;
  --color-sev-medium: #956400;
  --color-sev-high: #9F2F2D;
  --color-sev-critical: #6C1B1A;
  --color-info: #1F6C9F;
}

@custom-variant dark (&:where(.dark, .dark *));

.dark {
  --color-bg: #121412;
  --color-bg-2: #181B18;
  --color-panel: #1C1F1C;
  --color-border: #2A2E2A;
  --color-fg: #ECEEEA;
  --color-fg-2: #C9CDC7;
  --color-muted: #9AA097;
  --color-accent: #5DA37F;
  --color-accent-h: #6FB48F;
  --color-accent-fg: #0E1410;
}

@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after { animation-duration: 0.01ms !important; transition-duration: 0.01ms !important; }
}

html, body, #root { height: 100%; }
body { background: var(--color-bg); color: var(--color-fg); font-family: var(--font-sans); }
```

`web/src/main.tsx`:
```tsx
import { StrictMode } from "react";
import { createRoot } from "react-dom/client";
import "@/styles/tokens.css";
import { App } from "@/app/App";

createRoot(document.getElementById("root")!).render(
  <StrictMode>
    <App />
  </StrictMode>,
);
```

`web/src/app/App.tsx`:
```tsx
export function App() {
  return (
    <main className="mx-auto max-w-[1400px] p-8">
      <h1 className="text-3xl font-semibold tracking-tight">LucidFence 2.0</h1>
      <p className="mt-2 max-w-[65ch] text-muted">Dashboard en construcción. La API responde en /api/v1/health.</p>
    </main>
  );
}
```

`web/src/app/App.test.tsx`:
```tsx
import { render, screen } from "@testing-library/react";
import { App } from "./App";

test("muestra el título", () => {
  render(<App />);
  expect(screen.getByRole("heading", { name: "LucidFence 2.0" })).toBeInTheDocument();
});
```

- [ ] **Step 8: Instalar, probar y compilar**

```bash
cd /Users/adri/lucidfence-v2/web
npm install
npm run lint && npm run typecheck && npm test && npm run build
ls ../internal/web/dist
cd ..
git status --short internal/web/dist
```

Expected: test verde; `dist` contiene `index.html`, `assets/`, `fonts/` y `placeholder.html`; `git status` no muestra nada bajo `internal/web/dist` salvo `placeholder.html` (ya añadido).

- [ ] **Step 9: Test npm allowlist y arquitectura vuelven a pasar**

Run: `go test ./internal/arch/ -v`
Expected: `TestNpmDependencyAllowlist` PASS (ya no SKIP). Si falla por una devDependency auxiliar, añadirla a `allowlist_npm.txt` en este mismo commit y anotarlo en el mensaje.

- [ ] **Step 10: Commit**

```bash
git add web internal/web
git commit -q -m "feat(web): esqueleto Vite+React+TS+Tailwind con tokens Forest y embed en el binario

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01GjMTkpr4PnhrZTzqQ7J75w"
```

---

### Task 7: Batería runtime mínima y scripts de cobertura

**Files:**
- Create: `internal/battery/battery.go`, `internal/battery/checks_m0.go`, `internal/battery/battery_test.go`
- Create: `cmd/battery/main.go`
- Create: `scripts/coverage.sh`, `scripts/battery.sh`

**Interfaces:**
- Produces:
  ```go
  type Env struct { Bin string; Tmp string; BaseURL string }  // BaseURL vacío hasta M1
  type Check struct { Name string; Run func(ctx context.Context, env *Env) error }
  func Checks() []Check                       // orden de ejecución
  func Run(ctx context.Context, env *Env, checks []Check, w io.Writer) (passed, total int)
  ```
  Salida: una línea `PASS <name>` o `FAIL <name>: <err>` por check y al final `RUNTIME: <passed>/<total>`.

- [ ] **Step 1: Test de `Run` (falla)**

`internal/battery/battery_test.go`:
```go
package battery

import (
	"bytes"
	"context"
	"errors"
	"strings"
	"testing"
)

func TestRunCuentaPassYFail(t *testing.T) {
	checks := []Check{
		{Name: "ok", Run: func(context.Context, *Env) error { return nil }},
		{Name: "ko", Run: func(context.Context, *Env) error { return errors.New("boom") }},
	}
	var out bytes.Buffer
	passed, total := Run(context.Background(), &Env{}, checks, &out)
	if passed != 1 || total != 2 {
		t.Fatalf("passed=%d total=%d", passed, total)
	}
	s := out.String()
	for _, want := range []string{"PASS ok", "FAIL ko: boom", "RUNTIME: 1/2"} {
		if !strings.Contains(s, want) {
			t.Fatalf("salida %q sin %q", s, want)
		}
	}
}

func TestChecksM0IncluyeVersion(t *testing.T) {
	names := map[string]bool{}
	for _, c := range Checks() {
		names[c.Name] = true
	}
	if !names["version imprime lucidfence y la versión"] {
		t.Fatalf("checks=%v", names)
	}
}
```

- [ ] **Step 2: Ejecutar y ver que falla**

Run: `go test ./internal/battery/`
Expected: `undefined: Check`.

- [ ] **Step 3: Implementar `battery.go` y `checks_m0.go`**

`internal/battery/battery.go`:
```go
// Package battery es la batería runtime de LucidFence: cada claim de producto
// se ejecuta contra el binario real (spec §9.1 paso 5). CI la corre; el
// resultado es RUNTIME: N/N.
package battery

import (
	"context"
	"fmt"
	"io"
	"os/exec"
	"strings"
)

// Env es el entorno compartido por los checks.
type Env struct {
	Bin     string // ruta al binario lucidfence compilado
	Tmp     string // directorio temporal para datos
	BaseURL string // http://127.0.0.1:<port> cuando hay servidor (M1+)
}

// Check es un claim verificable.
type Check struct {
	Name string
	Run  func(ctx context.Context, env *Env) error
}

// Run ejecuta los checks en orden e imprime el tally.
func Run(ctx context.Context, env *Env, checks []Check, w io.Writer) (passed, total int) {
	for _, c := range checks {
		total++
		if err := c.Run(ctx, env); err != nil {
			fmt.Fprintf(w, "FAIL %s: %v\n", c.Name, err)
			continue
		}
		passed++
		fmt.Fprintf(w, "PASS %s\n", c.Name)
	}
	fmt.Fprintf(w, "RUNTIME: %d/%d\n", passed, total)
	return passed, total
}

// Checks devuelve todos los checks registrados, en orden.
func Checks() []Check {
	return checksM0()
}

func runBin(ctx context.Context, env *Env, args ...string) (string, error) {
	out, err := exec.CommandContext(ctx, env.Bin, args...).CombinedOutput()
	return strings.TrimSpace(string(out)), err
}
```

`internal/battery/checks_m0.go`:
```go
package battery

import (
	"context"
	"fmt"
	"strings"
)

func checksM0() []Check {
	return []Check{
		{Name: "version imprime lucidfence y la versión", Run: checkVersion},
	}
}

func checkVersion(ctx context.Context, env *Env) error {
	out, err := runBin(ctx, env, "version")
	if err != nil {
		return fmt.Errorf("%v: %s", err, out)
	}
	if !strings.HasPrefix(out, "lucidfence 2.") {
		return fmt.Errorf("salida inesperada: %q", out)
	}
	return nil
}
```

`cmd/battery/main.go`:
```go
// Command battery ejecuta la batería runtime contra un binario lucidfence.
package main

import (
	"context"
	"flag"
	"fmt"
	"os"
	"time"

	"github.com/adrimg3196/lucidfence/internal/battery"
)

func main() {
	bin := flag.String("bin", "bin/lucidfence", "ruta al binario lucidfence")
	timeout := flag.Duration("timeout", 3*time.Minute, "tiempo máximo total")
	flag.Parse()
	tmp, err := os.MkdirTemp("", "lucidfence-battery-*")
	if err != nil {
		fmt.Fprintln(os.Stderr, err)
		os.Exit(1)
	}
	defer os.RemoveAll(tmp)
	ctx, cancel := context.WithTimeout(context.Background(), *timeout)
	defer cancel()
	passed, total := battery.Run(ctx, &battery.Env{Bin: *bin, Tmp: tmp}, battery.Checks(), os.Stdout)
	if passed != total {
		os.Exit(1)
	}
}
```

- [ ] **Step 4: Ejecutar tests (pasan) y la batería real**

```bash
go test ./internal/battery/ -v
go build -o bin/lucidfence ./cmd/lucidfence && go run ./cmd/battery -bin bin/lucidfence
```

Expected: tests PASS; salida `PASS version imprime lucidfence y la versión` y `RUNTIME: 1/1`.

- [ ] **Step 5: Scripts de cobertura y batería**

`scripts/coverage.sh`:
```bash
#!/usr/bin/env bash
# Suelos de cobertura por paquete (spec §9.1 paso 4): domain y engine 85 %,
# resto 70 %, cmd/battery exento. Falla si algún paquete queda por debajo o
# no tiene tests.
set -euo pipefail
cd "$(dirname "$0")/.."
go test -race -count=1 -covermode=atomic -coverprofile=coverage.out ./... | tee test.out
status=0
while IFS= read -r line; do
  pkg=$(awk '{print $2}' <<<"$line")
  case "$pkg" in
    */cmd/battery) continue ;;
    */internal/domain/*|*/internal/domain|*/internal/engine|*/internal/engine/*) floor=85 ;;
    *) floor=70 ;;
  esac
  if grep -q '\[no test files\]' <<<"$line"; then
    echo "COVERAGE: $pkg sin tests (mínimo $floor%)"; status=1; continue
  fi
  pct=$(grep -oE 'coverage: [0-9.]+%' <<<"$line" | grep -oE '[0-9.]+' || echo 0)
  if awk -v p="$pct" -v f="$floor" 'BEGIN{exit !(p+0 < f)}'; then
    echo "COVERAGE: $pkg ${pct}% < ${floor}%"; status=1
  fi
done < <(grep -E '^(ok|\?)\s' test.out)
[ "$status" -eq 0 ] && echo "COVERAGE: OK"
exit "$status"
```

`scripts/battery.sh`:
```bash
#!/usr/bin/env bash
# Compila (si hace falta) y ejecuta la batería runtime contra el binario real.
set -euo pipefail
cd "$(dirname "$0")/.."
bin="${1:-bin/lucidfence}"
[ -x "$bin" ] || CGO_ENABLED=0 go build -o "$bin" ./cmd/lucidfence
go run ./cmd/battery -bin "$bin"
```

```bash
chmod +x scripts/coverage.sh scripts/battery.sh
scripts/coverage.sh
scripts/battery.sh
```

Expected: `COVERAGE: OK` y `RUNTIME: 1/1`.

- [ ] **Step 6: Actualizar `ARCHITECTURE.md` (ya lista `cmd/battery` e `internal/battery`) y commit**

```bash
go test ./internal/arch/
git add internal/battery cmd/battery scripts
git commit -q -m "feat(ci): batería runtime mínima y suelos de cobertura por paquete

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01GjMTkpr4PnhrZTzqQ7J75w"
```

---

### Task 8: `ci.yml`, raíles de agentes, CODEOWNERS y plantilla de PR

**Files:**
- Create: `.github/workflows/ci.yml`, `.github/workflows/agent-pr.yml`, `.github/workflows/agent-automerge.yml`
- Create: `.github/CODEOWNERS`, `.github/pull_request_template.md`, `.gitleaks.toml`

**Interfaces:**
- Produces: jobs de CI con nombres exactos `go-lint`, `go-test`, `web`, `battery`, `security` (la Task 12 los exige como checks obligatorios).

- [ ] **Step 1: `ci.yml`**

```yaml
name: CI

on:
  push:
    branches: ["**"]
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  go-lint:
    name: go-lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v7
        with: { go-version-file: go.mod }
      - name: gofmt
        run: test -z "$(gofmt -l .)" || { gofmt -l .; exit 1; }
      - run: go vet ./...
      - uses: golangci/golangci-lint-action@v9
        with: { version: v2.13.2 }

  go-test:
    name: go-test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v7
        with: { go-version-file: go.mod }
      - name: tests, arquitectura y suelos de cobertura
        run: scripts/coverage.sh

  web:
    name: web
    runs-on: ubuntu-latest
    defaults: { run: { working-directory: web } }
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with: { node-version: 24, cache: npm, cache-dependency-path: web/package-lock.json }
      - run: npm ci
      - run: npm run lint
      - run: npm run typecheck
      - run: npm test
      - run: npm run build
      - uses: actions/upload-artifact@v7
        with: { name: web-dist, path: internal/web/dist, retention-days: 1 }

  battery:
    name: battery
    needs: [web]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v7
        with: { go-version-file: go.mod }
      - uses: actions/download-artifact@v7
        with: { name: web-dist, path: internal/web/dist }
      - name: compilar binario con dashboard embebido
        run: CGO_ENABLED=0 go build -trimpath -o bin/lucidfence ./cmd/lucidfence
      - name: batería runtime
        run: scripts/battery.sh bin/lucidfence

  security:
    name: security
    runs-on: ubuntu-latest
    permissions: { contents: read, security-events: write }
    steps:
      - uses: actions/checkout@v7
        with: { fetch-depth: 0 }
      - uses: gitleaks/gitleaks-action@v3
        env: { GITHUB_TOKEN: "${{ secrets.GITHUB_TOKEN }}" }
      - uses: actions/setup-go@v7
        with: { go-version-file: go.mod }
      - run: go install golang.org/x/vuln/cmd/govulncheck@latest && govulncheck ./...
      - uses: actions/setup-node@v7
        with: { node-version: 24 }
      - run: cd web && npm ci --ignore-scripts && npm audit --audit-level=high
```

`.gitleaks.toml`:
```toml
[extend]
useDefault = true

[allowlist]
paths = ['''web/package-lock.json''', '''internal/web/dist/''']
```

- [ ] **Step 2: Raíles de agentes**

`.github/workflows/agent-pr.yml`:
```yaml
name: agent-pr

on:
  push:
    branches: ["agent/**"]

permissions:
  contents: read
  pull-requests: write

jobs:
  open:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: abrir PR si no existe
        env:
          GH_TOKEN: ${{ secrets.AGENTS_GITHUB_PAT }}
        run: |
          branch="${GITHUB_REF#refs/heads/}"
          if [ "$(gh pr list --head "$branch" --state open --json number --jq 'length')" = "0" ]; then
            title="$(git log -1 --format=%s)"
            gh pr create --head "$branch" --base main --title "$title" --label agent --body-file .github/pull_request_template.md
          fi
```

`.github/workflows/agent-automerge.yml`:
```yaml
name: agent-automerge

on:
  workflow_run:
    workflows: [CI]
    types: [completed]

permissions:
  contents: write
  pull-requests: write

jobs:
  merge:
    if: github.event.workflow_run.conclusion == 'success' && github.event.workflow_run.event == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: mergear si cumple los límites (spec §9.3)
        env:
          GH_TOKEN: ${{ secrets.AGENTS_GITHUB_PAT }}
          SHA: ${{ github.event.workflow_run.head_sha }}
        run: |
          set -euo pipefail
          num=$(gh pr list --search "$SHA" --state open --json number --jq '.[0].number // empty')
          [ -n "$num" ] || { echo "sin PR abierta para $SHA"; exit 0; }
          pr=$(gh pr view "$num" --json labels,additions,deletions,headRefName)
          labels=$(jq -r '.labels[].name' <<<"$pr")
          grep -qx agent <<<"$labels" || { echo "PR #$num no es de agente"; exit 0; }
          if grep -qx needs-human <<<"$labels"; then gh pr comment "$num" -b "automerge: etiqueta needs-human presente, espera revisión humana."; exit 0; fi
          size=$(( $(jq .additions <<<"$pr") + $(jq .deletions <<<"$pr") ))
          if [ "$size" -gt 400 ]; then gh pr comment "$num" -b "automerge: $size líneas cambiadas > 400. Divide la PR (spec §9.3)."; exit 0; fi
          protected=$(grep -vE '^\s*(#|$)' .github/CODEOWNERS | awk '{print $1}' | sed 's#^/##')
          changed=$(gh pr diff "$num" --name-only)
          for p in $protected; do
            prefix="${p%\*}"
            if grep -q "^${prefix}" <<<"$changed"; then
              gh pr comment "$num" -b "automerge: toca ${p} (CODEOWNERS); requiere aprobación del propietario."; exit 0
            fi
          done
          gh pr merge "$num" --squash --auto
          echo "automerge: PR #$num en cola de merge"
```

- [ ] **Step 3: CODEOWNERS y plantilla**

`.github/CODEOWNERS`:
```
# Ficheros que exigen aprobación del propietario aunque la CI esté verde (spec §9.2).
/ARCHITECTURE.md              @adrimg3196
/.github/                     @adrimg3196
/go.mod                       @adrimg3196
/go.sum                       @adrimg3196
/web/package.json             @adrimg3196
/internal/arch/               @adrimg3196
/internal/engine/guardrails*  @adrimg3196
/scripts/coverage.sh          @adrimg3196
```

`.github/pull_request_template.md`:
```markdown
## Qué cambia

## Por qué

## Cómo se ha verificado
- [ ] `make verify` en verde en local
- [ ] Nuevo claim de producto → check en `internal/battery`
- [ ] Cambio de API → `docs/openapi.yaml` actualizado
- [ ] Cambio de paquete → `ARCHITECTURE.md` actualizado

## Límites
- [ ] ≤ 400 líneas cambiadas (PRs de agente)
- [ ] No toca ficheros CODEOWNERS (o lleva `needs-human`)
```

- [ ] **Step 4: Crear las etiquetas en GitHub**

```bash
gh label create agent --color 0E8A16 --description "PR abierta por un agente (raíl agent-pr)" --force
gh label create needs-human --color B60205 --description "Bloquea el automerge: revisión humana" --force
gh label create v1-triage --color FBCA04 --description "Issue de 1.x pendiente de re-triaje tras 2.0" --force
gh label list --json name --jq '.[].name' | grep -E '^(agent|needs-human|v1-triage)$'
```

Expected: las tres etiquetas listadas.

- [ ] **Step 5: Validar sintaxis de los workflows y commit**

```bash
for f in .github/workflows/*.yml; do python3 -c "import yaml,sys; yaml.safe_load(open('$f'))" && echo "OK $f"; done
git add .github .gitleaks.toml
git commit -q -m "ci: gate ci.yml (lint, test, web, battery, security), raíles agent-pr/automerge y CODEOWNERS

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01GjMTkpr4PnhrZTzqQ7J75w"
```

---

### Task 9: Documentación de transición

**Files:**
- Create: `README.md`, `CHANGELOG.md`, `SECURITY.md`, `CONTRIBUTING.md`

- [ ] **Step 1: `README.md`**

```markdown
# LucidFence

**Geofencing multi-UEM local-first, gratis y open source (Apache-2.0).** Motor de
riesgo explicable sobre los UEM que ya tienes (Applivery, Intune, Jamf, Fleet,
Workspace ONE). Tu dato de ubicación no sale de tu máquina.

> **Estado: LucidFence 2.0 en construcción.** `main` contiene la reescritura en
> Go. La última versión estable es **1.6.1** (Python): releases en
> [GitHub Releases](https://github.com/adrimg3196/lucidfence/releases), código
> en la rama [`legacy/python`](https://github.com/adrimg3196/lucidfence/tree/legacy/python)
> y tag `v1.6.1-python-final`. Homebrew, Docker y la vitrina siguen sirviendo 1.6.1
> hasta la release 2.0.0.

## Por qué 2.0

Un binario en Go, sin runtime que instalar; dashboard React embebido; el
repositorio contiene solo producto. Diseño completo en
`docs/superpowers/specs/2026-09-05-lucidfence-2-go-rewrite-design.md`;
arquitectura normativa en `ARCHITECTURE.md`.

## Desarrollo

```bash
brew install go golangci-lint      # Go 1.27+
make web                            # compila el dashboard (Node 24+)
make build && ./bin/lucidfence version
make verify                         # el gate completo: lint, tests, cobertura, web, batería
```

## Principios

Local-first y cero telemetría · complemento del UEM, nunca un UEM · el admin
decide el runtime (`observe` por defecto, doble llave para `wipe`) · gratis y
open source · todo claim verificado en vivo.

## Licencia

Apache-2.0. Ver `LICENSE`.
```

- [ ] **Step 2: `CHANGELOG.md`, `SECURITY.md`, `CONTRIBUTING.md`**

`CHANGELOG.md`:
```markdown
# Changelog

Formato Keep a Changelog; versionado semántico.

## [2.0.0-dev] — en construcción

### Cambiado
- Reescritura completa en Go con un único binario y dashboard React embebido.
  El código 1.x queda en la rama `legacy/python` y el tag `v1.6.1-python-final`.
- La oficina de agentes (loops, GTM, recon, brand, merge-train) sale del
  repositorio. Quedan solo `ci.yml`, `agent-pr.yml` y `agent-automerge.yml`.

### Eliminado
- Empresa autónoma, atomicmail, whitelabel/FreeDomain, chat de IA, DDM/DSC,
  SSF/CAEP, predicción, HA por lease, app macOS Swift, worker Cloudflare,
  deploy Fly.io y publicación en PyPI (spec §4.2).

## [1.6.1] — 2026-09-04

Última release de la línea Python. Historial completo en
`legacy/python:CHANGELOG.md`.
```

`SECURITY.md`:
```markdown
# Seguridad

Si encuentras una vulnerabilidad, escribe a adrimg3196@gmail.com con asunto
"LucidFence security" o abre un aviso privado en
https://github.com/adrimg3196/lucidfence/security/advisories/new. No abras una
issue pública. Respuesta en 72 h.

Alcance: el binario `lucidfence`, el dashboard embebido y los workflows de este
repositorio. Las releases 1.x (rama `legacy/python`) reciben solo correcciones
críticas hasta la publicación de 2.0.0.

Invariantes que consideramos fallos de seguridad si se rompen: el producto
nunca actúa sobre un dispositivo real sin `enforce` explícito del admin; `wipe`
exige doble llave; las credenciales UEM nunca aparecen en respuestas GET, logs,
MCP ni frontend; ninguna URL de salida se usa sin pasar por la allowlist de egress.
```

`CONTRIBUTING.md`:
```markdown
# Contribuir

1. `make verify` debe estar en verde antes de abrir la PR. Es el mismo gate que
   corre la CI (`.github/workflows/ci.yml`).
2. Lee `ARCHITECTURE.md`: las fronteras entre paquetes y los límites de tamaño
   los valida la CI. Un paquete nuevo se documenta en la tabla de paquetes en el
   mismo commit.
3. Todo claim de producto nuevo añade un check a `internal/battery`. Todo cambio
   de API actualiza `docs/openapi.yaml`.
4. Dependencias nuevas: solo con entrada en `internal/arch/allowlist_*.txt` y
   aprobación del propietario (CODEOWNERS).
5. Agentes: rama `agent/<tema>`, PR automática, automerge si CI verde, ≤ 400
   líneas y sin tocar ficheros CODEOWNERS. Todo lo demás lleva `needs-human`.
6. Commits: Conventional Commits en español (`feat(engine): ...`).
```

- [ ] **Step 3: Commit**

```bash
git add README.md CHANGELOG.md SECURITY.md CONTRIBUTING.md
git commit -q -m "docs: README de transición, CHANGELOG 2.0.0-dev, SECURITY y CONTRIBUTING

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01GjMTkpr4PnhrZTzqQ7J75w"
```

---

### Task 10: Verificación local completa y CI verde en la rama

- [ ] **Step 1: `make verify`**

```bash
make verify
```

Expected: termina con `verify: OK`. Si `golangci-lint` señala algo, corregirlo en el fichero indicado y repetir.

- [ ] **Step 2: Push de la rama y espera de CI**

```bash
git push -u origin v2/day0
sleep 20
run_id=$(gh run list --branch v2/day0 --workflow CI --limit 1 --json databaseId --jq '.[0].databaseId')
gh run watch "$run_id" --exit-status
```

Expected: `gh run watch` termina con éxito y los cinco jobs (`go-lint`, `go-test`, `web`, `battery`, `security`) en verde. Si un job falla, abrir el log con `gh run view "$run_id" --log-failed`, corregir, commitear y volver a este paso.

---

### Task 11: Cerrar PRs 1.x, etiquetar issues, issue fijada

- [ ] **Step 1: Cerrar las 14 PRs abiertas con comentario estándar**

```bash
msg='Cerrada por la reescritura de LucidFence 2.0 en Go (spec: docs/superpowers/specs/2026-09-05-lucidfence-2-go-rewrite-design.md en main). El código 1.x sigue en la rama legacy/python y el tag v1.6.1-python-final. Si la idea de esta PR aplica a 2.0, ábrela como issue con la etiqueta enhancement. Gracias.'
for n in 418 417 416 415 414 413 412 410 409 405 403 402 400 290; do
  gh pr close "$n" --comment "$msg" && echo "cerrada #$n"
done
gh pr list --state open --json number --jq 'length'
```

Expected: `0` PRs abiertas (si entre tanto se abrió alguna nueva, cerrarla con el mismo mensaje).

- [ ] **Step 2: Etiquetar las issues abiertas**

```bash
for n in $(gh issue list --state open --limit 200 --json number --jq '.[].number'); do
  gh issue edit "$n" --add-label v1-triage >/dev/null && echo "etiquetada #$n"
done
gh issue list --state open --label v1-triage --limit 200 --json number --jq 'length'
```

Expected: el mismo número que issues abiertas (57 a fecha de la spec).

- [ ] **Step 3: Issue fijada anunciando 2.0**

```bash
cat > /tmp/issue-2.0.md <<'EOF'
LucidFence se reescribe entero en **Go** (un binario, dashboard React embebido, repo solo producto). La decisión y el diseño completo están en `docs/superpowers/specs/2026-09-05-lucidfence-2-go-rewrite-design.md`.

**Qué cambia hoy**
- `main` contiene 2.0 en construcción. Cada hito publica una pre-release `2.0.0-alpha.N`.
- El código 1.x vive en la rama `legacy/python` y el tag `v1.6.1-python-final`. La release 1.6.1 sigue disponible en Homebrew, Docker y GitHub Releases.
- Las PRs abiertas se han cerrado; las issues abiertas llevan `v1-triage` y se re-triajan al publicar 2.0.0.

**Hitos**: M0 día 0 · M1 núcleo demo · M2 riesgo y acciones · M3 conectores · M4 informes y auth · M5 público y release.
EOF
url=$(gh issue create --title "LucidFence 2.0: reescritura en Go (estado y hitos)" --body-file /tmp/issue-2.0.md --label enhancement)
echo "$url"
gh issue pin "${url##*/}"
```

Expected: la issue aparece fijada en la portada del repo.

---

### Task 12: Corte de `main` y protección de rama

- [ ] **Step 1: Absorber cualquier avance de `main` sin traer su contenido**

```bash
cd /Users/adri/lucidfence-v2
git fetch origin main
git merge -s ours origin/main -m "chore: absorber el último main 1.x en el corte 2.0 (estrategia ours; el árbol es el de v2/day0)

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01GjMTkpr4PnhrZTzqQ7J75w"
git diff --stat HEAD~1 HEAD | tail -1
git ls-files | grep -c '\.py$' || true
```

Expected: el diff de la fusión no cambia ficheros (árbol idéntico al de la rama) y el conteo de `.py` es `0`.

- [ ] **Step 2: Push a la rama, CI verde, y fast-forward de `main`**

```bash
git push origin v2/day0
sleep 20
run_id=$(gh run list --branch v2/day0 --workflow CI --limit 1 --json databaseId --jq '.[0].databaseId')
gh run watch "$run_id" --exit-status
git push origin v2/day0:main
git log --oneline -3 origin/main
```

Expected: el push a `main` es fast-forward (sin `--force`) y `origin/main` apunta al commit de fusión.

- [ ] **Step 3: Protección de rama con los checks nuevos y CODEOWNERS**

```bash
gh api -X PUT repos/adrimg3196/lucidfence/branches/main/protection \
  --input - <<'EOF'
{
  "required_status_checks": { "strict": true, "contexts": ["go-lint", "go-test", "web", "battery", "security"] },
  "enforce_admins": false,
  "required_pull_request_reviews": { "required_approving_review_count": 0, "require_code_owner_reviews": true, "dismiss_stale_reviews": true },
  "restrictions": null,
  "allow_force_pushes": false,
  "allow_deletions": false,
  "required_linear_history": false
}
EOF
gh api repos/adrimg3196/lucidfence/branches/main/protection --jq '{checks: .required_status_checks.contexts, codeowners: .required_pull_request_reviews.require_code_owner_reviews}'
```

Expected: `{"checks":["go-lint","go-test","web","battery","security"],"codeowners":true}`.

- [ ] **Step 4: Comprobar que los workflows 1.x han desaparecido y CI corre en `main`**

```bash
gh workflow list
sleep 20
gh run list --branch main --limit 3
```

Expected: solo `CI`, `agent-pr` y `agent-automerge` en la lista (los workflows 1.x con schedule aparecen como desactivados o no aparecen; si alguno sigue "active", desactivarlo con `gh workflow disable <nombre>`), y un run de `CI` en `main` en verde.

- [ ] **Step 5: Limpieza de ramas de trabajo**

```bash
git checkout -q main 2>/dev/null || git checkout -q -b main origin/main
git branch -D v2/day0
git push origin --delete v2/day0
git push origin --delete v2/design
git branch -r | grep -E 'v2/|legacy/'
```

Expected: en remoto solo `origin/legacy/python` (la spec ya está en `main`).

---

### Task 13: Cierre del hito

- [ ] **Step 1: Tag de hito**

```bash
git tag -a m0-dia-0 -m "M0: corte de main, esqueleto Go, gate de CI y raíles" && git push origin m0-dia-0
```

- [ ] **Step 2: Mensaje al propietario** (contenido mínimo del informe de cierre):
  - `main` es 2.0; 1.x en `legacy/python` y `v1.6.1-python-final`; CI verde con cinco checks obligatorios y CODEOWNERS.
  - 14 PRs cerradas; issues etiquetadas `v1-triage`; issue fijada con URL.
  - Automatización pausada: `com.openmausbot.lf-daily` (cómo reactivar), tablero Hermes `lucidfence` archivado (backup en `~/lucidfence-kanban-backup-2026-09-05.tar.gz`, cómo importar).
  - Pendiente del propietario: reconfigurar los crons de Zero/Jules en OpenClaw que apunten a `merge-train` o a rutas Python del repo.
  - Siguiente: plan M1 `docs/superpowers/plans/2026-09-05-m1-nucleo-demo.md`.
