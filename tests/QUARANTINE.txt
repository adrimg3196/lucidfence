# QUARANTINE.txt — módulos de test conocidos-flaky que monitor-hourly IGNORA.
#
# Formato: un nombre de fichero por línea (p.ej. test_web_bundle.py). Líneas que
# empiezan por '#' son comentarios. Si un test de este fichero falla en
# monitor-hourly, NO se cuenta como dip de salud (se aísla para no generar
# ruido), pero el fallo queda registrado en la salida del runner para que el
# dueño lo vea en el reporte semanal.
#
# Para sacar un test de cuarentena (p.ej. ya se arregló el bug real), bórralo de
# aquí y confírmalo. Para añadir uno nuevo, añade la línea y justifica en el PR.
#
# Estado 2026-08-24: sin entradas. Las dos que traia la PR original
# (test_web_bundle.py, test_ssf_transmitter.py) se retiraron: su fallo no
# era flakiness sino el bug real de serializacion P-256 (RFC 7518 6.2.1),
# arreglado en #284; 8 pasadas de suite posteriores sin un solo fallo.
# Una entrada nueva debe llevar owner=, motivo=, caduca= e issue=#N
# (lo exige tests/test_quarantine_honesty.py).
