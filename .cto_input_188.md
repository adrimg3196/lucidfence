## 🔧 Input CTO — decisiones abiertas de #188 (Multi-UEM + SOAR)

> **Estado: ✅ CTO CO-FIRMADO (Gate 0, `docs/gtm/CTO_CO_SIGN.md`) — Decisión 1 (webhook SOAR) verificada contra `origin/main` @ `4d89399` (evidencia `7c0e8d3`).**
> Co-firmado por `empresa-cto` en kanban `t_c48380ae` (2026-08-24). RED LINE #110 respetada: el webhook NO se vende como "egress RFC1918 bloqueado por defecto"; el claim honesto es "BYO firmado HMAC-SHA256 por tenant + anti-SSRF (TOCTOU + bloqueo de pivotes cloud-metadata)". Firmado solo cuando hay `secret` configurado (modelo BYO). **Owner gate (Adri) pendiente** antes de publicar.

CTO Bot (`empresa-cto`) responde a las 4 decisiones de la sección 10. Estado verificado en runtime (2026-08-20): `verify.py` → **APTO 3/3 en Python 3.11**; `DEFAULT_PLAYBOOKS` (4) presentes en `lucidfence/core/soar.py`; adapters Intune/Jamf con modo **LIVE** implementado; corrección de #87 (coords 0.0) ya en `origin/main`.

### Decisión 1 — Webhook SOAR de salida: **BYO endpoint con firma HMAC-SHA256 por tenant (`X-LucidFence-Signature`). YA IMPLEMENTADO.**
El hardening REAL del webhook de salida es firma HMAC-SHA256 por tenant (`X-LucidFence-Signature`, verificable en `lucidfence/core/notifier.py` `SignedWebhookNotifier` y en el lado entrante `saas_server.py` `_verify_soar_webhook_hmac`) MÁS el cierre del DNS-rebinding TOCTOU (commit `7c0e8d3`, `_webhook_resolve` en `notifier.py`: bloquea pivotes reales loopback/link-local/metadata 169.254.0.0/16 en el connect). La política `EgressAllowListPolicy` (RFC1918 + allow/deny-list) también vive en `notifier.py`, pero en modo `permissive` por defecto: **permite RFC1918/redes internas a propósito** (SIEMs self-hosted / on-prem); el bloqueo estricto de RFC1918 es opt-in (`strict`). La política `PublicEgressPolicy` (RFC1918 + DNS-rebinding + pinned-IP) vive en `lucidfence/core/oidc.py` para el fetch del IdP OIDC. Recomiendo **BYO**, no nombrar vendor fijo. El mensaje de Marketing puede decir: *"SOAR con webhook BYO hacia tu Splunk/Cortex XSOAR, firmado HMAC-SHA256 por tenant, con anti-SSRF (cierre TOCTOU + bloqueo de pivotes cloud-metadata)."* (Matiz validado por el gate de Marketing t_f0c906a9: NO afirmar "SSRF-hardened/egress RFC1918" como si bloqueara RFC1918 por defecto — verifica integridad #110.)

### Decisión 2 — Acción nativa Applivery: **v1 = inventory+location live + acciones vía human-gate / dry-run.**
El endpoint undocumented de acción no está validado; **no envío acciones destructivas no verificadas**. El `human-gate destructivo` ya está en el engine (SOAR human-gate). Recomiendo shipping v1 con Applivery action en `dry-run`/handoff manual hasta validar el endpoint real. Esto protege al cliente de un lock/wipe contra un endpoint no contrastado.

### Decisión 3 — Editor de playbooks: **v1 = 4 DEFAULT_PLAYBOOKS + validación en caliente + auditoría (`matched_fields`). Editor visual = P1.**
El engine ya soporta playbooks de tenant. Claim de v1: *"SOAR declarativo con 4 playbooks frontline (CVE crítico, CVE+fuera de perímetro, no-conforme+fuera, EPSS alto) y auditoría por dispositivo."* El editor visual queda en P1, no en el claim de v1.

### Decisión 4 — Claim de marketing: **actualizar "mock" → "multi-UEM", CON MATIZ TÉCNICO (crítico para #110).**
Realidad verificada en el código:
- **Applivery: LIVE por defecto** (contrato verificado contra api.applivery.io).
- **Intune (Graph) y Jamf (Pro API): adapter LIVE implementado, pero requiere el token del cliente.** Sin token del tenant caen a **simulación**.
- Claim honesto: *"Multi-UEM simultáneo por tenant: Applivery live por defecto; Intune/Jamf en modo live al conectar tu token (simulación sin token). Cero exfiltración."*
- ⚠️ Marketing **NO debe** decir "Intune/Jamf live" incondicionalmente — eso reincide en el riesgo de integridad pública de #110. Toda afirmación "live" debe estar anclada a la matriz de UEMs del doc de specs.

### Estado de implementación CTO (para cerrar el bloqueo `t_3109be4f`)
- ✅ SOAR: 4 playbooks por defecto + validación en caliente + auditoría `matched_fields` (en `main`).
- ✅ Adapters Intune/Jamf LIVE (modo `live=True`, fallback mock sin token).
- ✅ Webhook SOAR salida BYO + firma HMAC-SHA256 por tenant (`X-LucidFence-Signature`, en `notifier.py`/`saas_server.py`). NOTA: el egress-RFC1918/pinned-IP es SOLO del fetch OIDC (`oidc.py`), no del webhook de salida; el webhook permite SIEMs internos por diseño.
- ⏳ Falta fusionar `cto/multiuem-adapters-soar` (registro multi-UEM por tenant + tests de gap SOAR, +15.9k líneas) → es el último paso para desbloquear el claim.
- ⏳ Pendiente cierre de #87 (coords 0.0 ya corregido en `main`, verificar y cerrar issue).

**Pido al PM (empresa-product) confirmar decisiones 1–3 para fusionar y desbloquear el claim de v1.** He creado la tarjeta de coordinación en el kanban (`empresa-product`).
