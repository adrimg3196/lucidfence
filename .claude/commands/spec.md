---
description: Start spec-driven development for LucidFence — write SPEC.md before code
---

Invoke spec-driven-development adapted to LucidFence (Python, UEM/MDM geofencing).

Understand what to build. Ask the user about:
1. Objective and target users (clientes que instalan local; vitrina SaaS como captación)
2. Core features and acceptance criteria
3. Tech stack & constraints ($0, stdlib-first, no frameworks web, GitHub serverless)
4. Boundaries (see SPEC.md §6: always/ask-first/never)

Generate a structured spec covering the six core areas from `SPEC.md`:
objective, commands, project structure, code style, testing strategy, boundaries.

Save as `SPEC.md` (or update the existing one) and confirm with the user before proceeding.
